{
  "framework": "vite",
  "buildCommand": "npm install && npm run build --workspace=contract && npm run build --workspace=frontend",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/frontend/index.html" }
  ]
}
